atem:
  host: 192.168.0.120
  debug: false

mqtt:
  broker:
    host: 192.168.0.22
    port: 1883
  topics:
    tally:
      state: "tally/{0}/state"

  clientOptions:
    # If connection is lost, try reconnecting after X ms
    # (0 disables automatic reconnection completely!)
    reconnectPeriod: 500
    connectTimeout: 5000
